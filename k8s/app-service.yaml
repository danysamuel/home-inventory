apiVersion: v1
kind: Service
metadata:
  name: home-inventory
  namespace: home-inventory
spec:
  selector:
    app: home-inventory
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
